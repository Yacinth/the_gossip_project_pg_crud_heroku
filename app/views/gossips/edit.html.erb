<br>
  <div class="col-lg-8 offset-lg-2 firm-group">
<h1>Editer votre Gossip</h1>
</div>
<br>
<%= form_tag url_for(action: 'create'), method: "post" do %>
  <div class="col-lg-8 offset-lg-2 firm-group">
    <%= label_tag 'Title' %>
    <%= text_field_tag 'title', @gossip.title, class: "form-control", placeholder: "Enter gossip's title" %>
  </div>
  <br>
  <div class="col-lg-8 offset-lg-2 firm-group">
    <%= label_tag 'Content' %>
    <%= text_area_tag 'content', @gossip.content, class: "form-control" %>
  </div>
  <br>
  <div class="d-flex justify-content-center">
    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
    <%= submit_tag "Update Gossip", class: "btn btn-primary"%>
  </div>
<% end %>
  
<br>
<% if @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
</div>
